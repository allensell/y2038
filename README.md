# 32-BIT CLOCK Overflow

This simple program shows the time left until the end!!

This is a very simple web script that just displays the time until the 32bit Linux/Unix time expires.  
That is basically the simple reason of this.  It also shows a lot of other basic information.
